{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		-Server
	}
	# Caddy сам выставляет X-Forwarded-For / X-Forwarded-Proto.
	reverse_proxy nginx:80
}
